#include <iostream>
#include <vector>

using namespace std;

vector<vector<int>> pytagoricTriple(int top){
	vector<vector<int>> triplesFound;

	for (int i =0; i <= top; i++){
		for(int x = 0; x < i ; x++){
//			for (int y = 0; ) optimization idea, gotta buy some stuff brb
		}
	}

	return triplesFound;
}
